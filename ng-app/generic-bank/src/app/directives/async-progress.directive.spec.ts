import { AsyncProgressDirective } from './async-progress.directive';

describe('AsyncProgressDirective', () => {
  it('should create an instance', () => {
    const directive = new AsyncProgressDirective();
    expect(directive).toBeTruthy();
  });
});
